[package]
name = "ginger-sc"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[dev-dependencies]
# Test-only: an independent OSC implementation, so the mock endpoint's packets are real OSC.
rosc = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
